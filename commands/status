#!/bin/bash

help() {
  echo "Usage:"
  echo "  swarm status [options]"
}

core() {
  docker stack services $@
}

run() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  core $@
}

run $@
